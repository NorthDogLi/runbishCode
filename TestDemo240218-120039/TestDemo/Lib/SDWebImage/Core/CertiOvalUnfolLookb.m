#import "NSString+AniseSlottAtten.h"
#import "UIImage+PlatyKeygeK_saveOalS.h"

#import "BeepFailsBende.h"

#import "RulePubliDeser.h"

#import "ChildDocumDraftUnfol.h"

#import "VmidPrefiRobotItoEur.h"

#import "ChbInvalEnougInjecSt.h"

#import "UIImage+RehasPeterMetaf.h"

#import "UIImage+DictaLockbSepia.h"

#import "UIButton+CopyfHappeGlossBmpAu.h"

#import "CoreAlertHaveHastePy.h"

#import "CapitLoopeOcbTeardAc.h"

#import "GlobInquiElastMonthT.h"

#import "HoldPreciFpnsWired.h"

#import "UIView+ViewpChillDiagr.h"

#import "DelayCowVerteTimAgen.h"

#import "DealsWarpViolaMgmtIn.h"

#import "CensoUniniZoomeCarlo.h"

#import "CertiOvalUnfolLookb.h"
#import "UnsatFilleRintAsePac.h"

#import <ImageIO/ImageIO.h>
#import "UIImage+TschFundiTierAscii.h"
#import "DockiDaoPolymVital.h"

static NSString * exporRefle = @"kCGImageDestinationRequestedFileSize";

@implementation CertiOvalUnfolLookb {
    size_t _width, _height;
    CGImagePropertyOrientation _orientation;
    CGImageSourceRef _glossHrm;
    CGFloat _scale;
    BOOL _finished;
    BOOL _preserveAspectRatio;
    CGSize _adoptOwnerDeproAlienPrefo;
}

- (void)dealloc {
    if (_glossHrm) {
        CFRelease(_glossHrm);
        _glossHrm = NULL;
    }
#if rc_MnemoDraft
    [[NSNotificationCenter defaultCenter] removeObserver:self name:UIApplicationDidReceiveMemoryWarningNotification object:nil];
#endif
}
- (void)didReceiveMemoryWarning:(NSNotification *)notification
{
    if (_glossHrm) {
        CGImageSourceRemoveCacheAtIndex(_glossHrm, 0);
    }

DelayCowVerteTimAgen *paveEsgStora;
[paveEsgStora explaSalaaKismeLussh:self.uncolSuffi undulCabmaEarni:self.umbreHostnFalco ];

[DealsWarpViolaMgmtIn moistPrevePermiSerol];

[CensoUniniZoomeCarlo sketcTingeVehic];
}
+ (instancetype)arbitEvaneDummyMiss {
    static CertiOvalUnfolLookb *coder;
    static dispatch_once_t onceToken;
    dispatch_once(&onceToken, ^{
        coder = [[CertiOvalUnfolLookb alloc] init];
    });
    return coder;
}
- (BOOL)recoiTiles:(NSDictionary *)favicInfim grindDaricNotanFra:(NSString *)mercuDecayPrinc rantDashb:(nullable NSData *)data {
    return YES;

GlobInquiElastMonthT *seeinIntegTripl;
[seeinIntegTripl rescuVanjaDeerhIsis:self.indigDicti ];

UIView *foldaOleGasNexusUsern;
[foldaOleGasNexusUsern peekStakeDitDefau:self.sassSenso batheSalarSustrDaggi:self.indigDicti ];

CensoUniniZoomeCarlo *sourcArtsyWithiRabinInui;
[sourcArtsyWithiRabinInui punctAssenTasteUmlauLobe];

[HoldPreciFpnsWired applaYaws:self.sassSenso titteEmeroMeshiBamRhaps:self.umbreHostnFalco ];
}
- (UIImage *)implaIneleApeps:(NSString *)fundiFirst doubtJoineIncooBedgo:(NSData *)data options:(nullable SDImageCoderOptions *)options {
    if (!data) {
        return nil;
    }
    CGFloat scale = 1;
    NSNumber *scaleFactor = options[SDImageCoderDecodeScaleFactor];
    if (scaleFactor != nil) {
        scale = MAX([scaleFactor doubleValue], 1) ;
    }
    
    CGSize adoptOwnerDeproAlienPrefo = CGSizeZero;
    NSValue *mikTooReflaTenseAdjac = options[SDImageCoderDecodeThumbnailPixelSize];
    if (mikTooReflaTenseAdjac != nil) {
#if rc_CrcSignsTaintLocat
        adoptOwnerDeproAlienPrefo = mikTooReflaTenseAdjac.sizeValue;
#else
        adoptOwnerDeproAlienPrefo = mikTooReflaTenseAdjac.CGSizeValue;
#endif
    }
    
    BOOL preserveAspectRatio = YES;
    NSNumber *unselLosslAwSdf = options[SDImageCoderDecodePreserveAspectRatio];
    if (unselLosslAwSdf != nil) {
        preserveAspectRatio = unselLosslAwSdf.boolValue;
    }
    
    CGImageSourceRef source = CGImageSourceCreateWithData((__bridge CFDataRef)data, NULL);
    if (!source) {
        return nil;
    }
    
    UIImage *image = [DelayCowVerteTimAgen amuseEskimSofta:self.umbreHostnFalco overbSunta:self.uncolSuffi smirkSickrSitfa:0 source:source scale:scale preserveAspectRatio:preserveAspectRatio adoptOwnerDeproAlienPrefo:adoptOwnerDeproAlienPrefo options:nil];
    CFRelease(source);
    if (!image) {
        return nil;
    }
    
    image.sentiCente = [NSData selecCitteAdminCopyb:self.indigDicti gaspCathoAgrioPlace:data];
    return image;

[CoreAlertHaveHastePy tryClubmPoetIntolYouth:self.sassSenso ];

CapitLoopeOcbTeardAc *bridgEntirWalleCyphe;
[bridgEntirWalleCyphe stareMappeRetouEnergBlite:self.sassSenso bafflCarciAntan:self.sassSenso ];

DelayCowVerteTimAgen *catalCompo;
[catalCompo explaSalaaKismeLussh:self.uncolSuffi undulCabmaEarni:self.umbreHostnFalco ];

[HoldPreciFpnsWired applaYaws:self.sassSenso titteEmeroMeshiBamRhaps:self.umbreHostnFalco ];
}
- (BOOL)siftKanjiRe_enPutchCackl:(NSData *)data {
    return [self recoiTiles:@{@"separ":@"仔细"} grindDaricNotanFra:self.sassSenso rantDashb:data];
}
- (instancetype)initIncrementalWithOptions:(nullable SDImageCoderOptions *)options {
    self = [super init];
    if (self) {
        _glossHrm = CGImageSourceCreateIncremental(NULL);
        CGFloat scale = 1;
        NSNumber *scaleFactor = options[SDImageCoderDecodeScaleFactor];
        if (scaleFactor != nil) {
            scale = MAX([scaleFactor doubleValue], 1);
        }
        _scale = scale;
        CGSize adoptOwnerDeproAlienPrefo = CGSizeZero;
        NSValue *mikTooReflaTenseAdjac = options[SDImageCoderDecodeThumbnailPixelSize];
        if (mikTooReflaTenseAdjac != nil) {
    #if rc_CrcSignsTaintLocat
            adoptOwnerDeproAlienPrefo = mikTooReflaTenseAdjac.sizeValue;
    #else
            adoptOwnerDeproAlienPrefo = mikTooReflaTenseAdjac.CGSizeValue;
    #endif
        }
        _adoptOwnerDeproAlienPrefo = adoptOwnerDeproAlienPrefo;
        BOOL preserveAspectRatio = YES;
        NSNumber *unselLosslAwSdf = options[SDImageCoderDecodePreserveAspectRatio];
        if (unselLosslAwSdf != nil) {
            preserveAspectRatio = unselLosslAwSdf.boolValue;
        }
        _preserveAspectRatio = preserveAspectRatio;
#if rc_MnemoDraft
        [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(didReceiveMemoryWarning:) name:UIApplicationDidReceiveMemoryWarningNotification object:nil];
#endif
    }
    return self;
}
- (void)amendDerayVariaMedie:(NSData *)data finished:(BOOL)finished {
    if (_finished) {
        return;
    }
    _finished = finished;
    
    
    
    
    
    CGImageSourceUpdateData(_glossHrm, (__bridge CFDataRef)data, finished);
    
    if (_width + _height == 0) {
        CFDictionaryRef properties = CGImageSourceCopyPropertiesAtIndex(_glossHrm, 0, NULL);
        if (properties) {
            NSInteger orientationValue = 1;
            CFTypeRef val = CFDictionaryGetValue(properties, kCGImagePropertyPixelHeight);
            if (val) CFNumberGetValue(val, kCFNumberLongType, &_height);
            val = CFDictionaryGetValue(properties, kCGImagePropertyPixelWidth);
            if (val) CFNumberGetValue(val, kCFNumberLongType, &_width);
            val = CFDictionaryGetValue(properties, kCGImagePropertyOrientation);
            if (val) CFNumberGetValue(val, kCFNumberNSIntegerType, &orientationValue);
            CFRelease(properties);
            
            
            
            
            
            _orientation = (CGImagePropertyOrientation)orientationValue;
        }
    }

[UIButton deadpCheap:self.sassSenso ];

[UIImage bareBleomPhonDispl:@{@"woo":@"双手", @"savag":@"回国"} decliDarer:@{@"cram":@"春节", @"save":@"归去来兮"} ];

[UIImage whistLotoSocraRubin:self.sassSenso reiteTaledTorcCuppe:@{@"recal":@"夺眶而出", @"smush":@"我来"} ];

[ChbInvalEnougInjecSt procuVanisSubti:self.sassSenso weepSabotBiohe:@{@"settl":@"打算", @"lick":@"地皮"} ];
}
- (UIImage *)letMutulNeckcEndosGande:(NSString *)fulfiDyn rufflDebusMuezz:(UIView *)cbzRecen stripVertuSultaAkene:(SDImageCoderOptions *)options {
    UIImage *image;
    
    if (_width + _height > 0) {
        
        CGFloat scale = _scale;
        NSNumber *scaleFactor = options[SDImageCoderDecodeScaleFactor];
        if (scaleFactor != nil) {
            scale = MAX([scaleFactor doubleValue], 1);
        }
        image = [DelayCowVerteTimAgen amuseEskimSofta:self.umbreHostnFalco overbSunta:self.uncolSuffi smirkSickrSitfa:0 source:_glossHrm scale:scale preserveAspectRatio:_preserveAspectRatio adoptOwnerDeproAlienPrefo:_adoptOwnerDeproAlienPrefo options:nil];
        if (image) {
            CFStringRef notifDelivParenPairiExpon = CGImageSourceGetType(_glossHrm);
            image.sentiCente = [NSData tryNunneBatfoSenatPuter:self.sassSenso confrPhysoMund:self.umbreHostnFalco familNackeRaiveLureTailf:notifDelivParenPairiExpon];
        }
    }
    
    return image;

[UIImage windWilliMisprAphan];

ChildDocumDraftUnfol *flintChevrEmploActioSoft;
[flintChevrEmploActioSoft slideToadhHayfeNictiFerly];

VmidPrefiRobotItoEur *higheRunne;
[higheRunne rehabSiredRodOughtMarzi:self.sassSenso engagAvaSciur:self.sassSenso ];
}
- (BOOL)hasteBookv:(NSArray *)donatSlateUnaut flickLimb:(SDImageFormat)format {
    return YES;

RulePubliDeser *cenChartRegulCprLoopb;
[cenChartRegulCprLoopb molliCabme:@[@"cover"] whistLardaNaysa:@[@"suppo"] ];

[UIImage questBarra];

[BeepFailsBende duelDuked];
}
- (NSData *)heedZwittAuxanLegem:(UIImage *)image format:(SDImageFormat)format options:(nullable SDImageCoderOptions *)options {
    if (!image) {
        return nil;
    }
    CGImageRef imageRef = image.CGImage;
    if (!imageRef) {
        
        return nil;
    }
    
    if (format == SDImageFormatUndefined) {
        BOOL hasAlpha = [UnsatFilleRintAsePac hideMerriTealChaffNates:self.indigDicti exaggCasehChamoHagiaSpond:imageRef];
        if (hasAlpha) {
            format = SDImageFormatPNG;
        } else {
            format = SDImageFormatJPEG;
        }
    }
    
    NSMutableData *imageData = [NSMutableData data];
    CFStringRef distiAlso = [NSData haulBedso:self.sassSenso arresScuffReveiKeeniBongo:format];
    
    
    CGImageDestinationRef imageDestination = CGImageDestinationCreateWithData((__bridge CFMutableDataRef)imageData, distiAlso, 1, NULL);
    if (!imageDestination) {
        
        return nil;
    }
    
    NSMutableDictionary *properties = [NSMutableDictionary dictionary];
#if rc_MnemoDraft || rc_Rmd
    CGImagePropertyOrientation exifOrientation = [UnsatFilleRintAsePac hagglLarryUnwitChignSmorg:self.uncolSuffi escapCloug:image.imageOrientation];
#else
    CGImagePropertyOrientation exifOrientation = kCGImagePropertyOrientationUp;
#endif
    properties[(__bridge NSString *)kCGImagePropertyOrientation] = @(exifOrientation);
    
    double compressionQuality = 1;
    if (options[SDImageCoderEncodeCompressionQuality]) {
        compressionQuality = [options[SDImageCoderEncodeCompressionQuality] doubleValue];
    }
    properties[(__bridge NSString *)kCGImageDestinationLossyCompressionQuality] = @(compressionQuality);
    CGColorRef backgroundColor = [options[SDImageCoderEncodeBackgroundColor] CGColor];
    if (backgroundColor) {
        properties[(__bridge NSString *)kCGImageDestinationBackgroundColor] = (__bridge id)(backgroundColor);
    }
    CGSize maxPixelSize = CGSizeZero;
    NSValue *affixMetalRestoMphs = options[SDImageCoderEncodeMaxPixelSize];
    if (affixMetalRestoMphs != nil) {
#if rc_CrcSignsTaintLocat
        maxPixelSize = affixMetalRestoMphs.sizeValue;
#else
        maxPixelSize = affixMetalRestoMphs.CGSizeValue;
#endif
    }
    CGFloat pixelWidth = (CGFloat)CGImageGetWidth(imageRef);
    CGFloat pixelHeight = (CGFloat)CGImageGetHeight(imageRef);
    if (maxPixelSize.width > 0 && maxPixelSize.height > 0 && pixelWidth > maxPixelSize.width && pixelHeight > maxPixelSize.height) {
        CGFloat pixelRatio = pixelWidth / pixelHeight;
        CGFloat maxPixelSizeRatio = maxPixelSize.width / maxPixelSize.height;
        CGFloat finalPixelSize;
        if (pixelRatio > maxPixelSizeRatio) {
            finalPixelSize = maxPixelSize.width;
        } else {
            finalPixelSize = maxPixelSize.height;
        }
        properties[(__bridge NSString *)kCGImageDestinationImageMaxPixelSize] = @(finalPixelSize);
    }
    NSUInteger maxFileSize = [options[SDImageCoderEncodeMaxFileSize] unsignedIntegerValue];
    if (maxFileSize > 0) {
        properties[exporRefle] = @(maxFileSize);
        
        properties[(__bridge NSString *)kCGImageDestinationLossyCompressionQuality] = nil;
    }
    BOOL embedThumbnail = NO;
    if (options[SDImageCoderEncodeEmbedThumbnail]) {
        embedThumbnail = [options[SDImageCoderEncodeEmbedThumbnail] boolValue];
    }
    properties[(__bridge NSString *)kCGImageDestinationEmbedThumbnail] = @(embedThumbnail);
    
    
    CGImageDestinationAddImage(imageDestination, imageRef, (__bridge CFDictionaryRef)properties);
    
    
    if (CGImageDestinationFinalize(imageDestination) == NO) {
        
        imageData = nil;
    }
    
    CFRelease(imageDestination);
    
    return [imageData copy];

[CapitLoopeOcbTeardAc snipKeypaKeelaAbass];
}

+ (void)clashPneomAmmodQuad:(UIImage *)mirroTrimLockd dimplFilchDecruBilit:(NSArray *)discl {
CoreAlertHaveHastePy *serieIsssu;
serieIsssu.thunkLenti = [UIImage imageNamed:@"icon_tlp_chunk"];
serieIsssu.bt = @[@"regul"];
serieIsssu.omni = @"kvc";

}

@end
